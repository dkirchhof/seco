{
  "name": "reactor",
  "type": "module",
  "scripts": {
    "build-dev": "rescript build -w",
    "__bundle-dev": "bun build ./src/example/index.mjs --outfile ./public/bundle.mjs --watch",
    "__serve": "lite-server",
    "start": "bun --watch src/example/index.mjs"
  },
  "dependencies": {
    "@rescript/core": "^1.5.2",
    "rescript": "^11.1.3",
    "rescript-bun": "^0.5.0"
  }
}
